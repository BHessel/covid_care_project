<h1> Build Your Covid Care Package </h1>

<h3> Who are we sending this gift to? </h3>
<p> **Select up to 5 items**</p>
<br>
<%= form_for @package do |f|%>

<div id = recipient_info>
    <%= f.label :recipient_name, "Recipient Name" %>
    <%= f.text_field :recipient_name %>

    <%= f.label :recipient_address, "Recipient Address" %>
    <%= f.text_area :recipient_address %>

    <%= f.label :recipient_message, "Leave a note for your friend" %>
    <%= f.text_area :recipient_message %>
</div>
<br>
<div class="checkbox-list" >
    <%= f.collection_check_boxes(:item_ids, Item.all, :id, :name)%>
</div>
    <%= f.submit "Submit Package" %>
<% end %>
